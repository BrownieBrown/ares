#include "core/common/Error.hpp"

// Error types are header-only, but we need this file for the build system
namespace ares::core {
// Intentionally empty - all implementations are in the header
} // namespace ares::core
